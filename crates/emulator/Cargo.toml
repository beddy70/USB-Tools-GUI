[package]
name = "edlink-emulator"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Émulateur virtuel Turbo EverDrive Pro (port série PTY) pour développer l'interface sans matériel"

[[bin]]
name = "edlink-emulator"
path = "src/main.rs"

[dependencies]
nix = { version = "0.26", features = ["term"] }
serde_json = "1"
